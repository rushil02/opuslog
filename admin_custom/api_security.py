""" User behaviour tracking, no other viable solutions. """
""" Dynamic url (signing token), based on upon a random number stored in session,
which is created only when the user has visited the first page. Decrypt and match the token with
random num, and return JSON. """
